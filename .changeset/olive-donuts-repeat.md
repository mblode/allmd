---
"allmd": patch
---

Reposition the package around context for agents. New description and `--help` line, and the npm README is now the same document as the repo README instead of a stale fork that still claimed Node 22.12.
